"use client";
/**
 * Export center — CSV/PDF reports for LGU/DOH submission.
 * CSV formatting for large datasets is offloaded to csvExport.worker.ts
 * via useWebWorker so the Export button never freezes the UI (§7.4).
 */
export default function ReportsPage() {
  return <div>Analytics Reports — TODO Phase 5</div>;
}
